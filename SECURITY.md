# Security

Report issues privately to the maintainer. Do not open a public issue for secrets.

- Never commit API keys. Use a local `.env` (gitignored).
- Provider keys live in the browser at runtime. They must not land in git.
