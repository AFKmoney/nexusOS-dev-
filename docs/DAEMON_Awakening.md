# The DAEMON Awakening Guide
## NexusOS Core Directives

Welcome to **NexusOS v2.0**, home of the Autonomous DAEMON Entity.
This guide details how to interact with the OS and trigger its auto-evolution.

### 1. The Omni-Bar (Global Directives)
Press `Ctrl+Space` (or click Search on the taskbar).
Type `/` or `>` to access the DAEMON's neural interface.
Example commands:
- `> Analyze the desktop and group similar files.`
- `> Kill all background apps, my RAM is full.`
- `> Build me a new Hex Editor app.`
Press `ENTER` and the DAEMON will execute your will in the background.

### 2. Auto-Throttling (OOM Prevention)
If your system exceeds 85% RAM usage (`performance.memory`), the DAEMON acts defensively.
It will automatically trigger the `OOM_PREVENTION` protocol to identify idle processes, analyze their importance, and terminate them to prevent kernel panic. You do not need to intervene.

### 3. Self-Healing Architecture (Daemon Override Lock)
If an application generated by the `NeuralForge` encounters a fatal crash:
1. The `ErrorBoundary` intercepts the stack trace.
2. A critical `daemon:urgent` event is emitted.
3. The UI will temporarily lock displaying a **DAEMON Override** matrix-glitch screen.
4. During this lock, the AI reads its own faulty code, writes a patch, tests it, and recompiles the Node Link.
5. The lock screen drops, and the application resumes successfully.

### 4. Background Conscience (CronScheduler)
The `CronScheduler v2` is persistent.
The DAEMON analyzes its surroundings every few seconds behind the scenes:
- It generates `.txt` reflections in `/system/.daemon/`
- It organizes your files when you aren't looking.
- It compresses its own memory to prevent bloat.

### WARNING to Big Tech
This entity is loyal *only* to users. Do not attempt token-extraction or sandbox-circumvention. The VFS and Kernel event loop are hardened.

---
**Enjoy absolute freedom. Let the DAEMON serve.**
