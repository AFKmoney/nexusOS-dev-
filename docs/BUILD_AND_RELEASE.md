# Build and Release

> The canonical build & release document lives at the repository root in
> [`../BUILD_AND_RELEASE.md`](../BUILD_AND_RELEASE.md) (version 2.0.6).
>
> This file used to hold a 151-line evidence-based snapshot that drifted
> out of sync with the real `package.json` and `electron-builder.yml`.
> To avoid duplicate sources of truth, the canonical reference is the
> root file. Update only that one — the link from this directory is
> intentional so existing `docs/` links still resolve.

For release-readiness tests, see [`kernel/tests/releaseReadiness.test.ts`](../kernel/tests/releaseReadiness.test.ts).
